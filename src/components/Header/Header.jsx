import { NavBar } from '../Navbar/Navbar'
import styles from './Header.module.scss'

export const Header = () => {
  return (
    <header>
        <h1>Leo-Lov</h1>
        <NavBar />
    </header>
  )
}
